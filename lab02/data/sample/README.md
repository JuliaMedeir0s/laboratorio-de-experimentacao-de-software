# Amostra (Sprint 1)

Este diretório deve conter os arquivos `.csv` gerados pelo CK para **um** repositório Java (conforme o requisito da Sprint 1).

Exemplo de comando:

- `python scripts/clonar_e_analisar.py --repo apache/commons-lang --output data/sample/`
